/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow all dev origins for preview
  allowedDevOrigins: [
    'run-agent-69d8ec1f2936cc54b56aa384-mntv2dur-preview.agent-sandbox-my-c1-gw.trae.ai',
    'localhost',
  ],
};

export default nextConfig;